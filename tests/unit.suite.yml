# Codeception Test Suite Configuration
#
# Suite for unit (internal) tests.

actor: UnitTester
modules:
  enabled:
     - Asserts
     - Db
     - Yii2:
         configFile: 'config/test.php'
         part: [init, orm, email]
